import { useSelector } from 'react-redux'
import cartItems from '../../assests/cartItems'
useSelector
const cartContainer = () => {
  return (
    <section>
      const {(total, amount, cartItems)} = useSelector((store)=> return
      store.cart)
    </section>
  )
}

export default cartContainer
